const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d 
const subtracao = d - b
const multiplicacao = a * b
const divisao = d/a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, -divisao, modulo)

// operadores unarios são operadores que só precisam de um fator, emplo o sinal de menos na frente da divisão